CREATE TABLE DOCUMENTATION(
  id INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1, INCREMENT BY 1) PRIMARY KEY,
  policy    VARCHAR(100) ,
  document_data VARCHAR(1000) ,
  document_type VARCHAR(100) 
);

